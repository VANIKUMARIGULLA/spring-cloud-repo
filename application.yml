server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VANIKUMARIGULLA/spring-cloud-repo
          clone-on-start: true
          default-label: main
